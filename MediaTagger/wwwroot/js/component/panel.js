import {ComponentBase} from '../../drjs/browser/component.js';

export class PanelComponent extends ComponentBase{
    constructor(selector, htmlName='panel') {
        super(selector,htmlName);
    }
}

export default PanelComponent;