import {ComponentBase} from '../../drjs/browser/component.js';

export class HomeComponent extends ComponentBase{
    constructor(selector, htmlName='home') {
        super(selector,htmlName);
    }
}

export default HomeComponent;