import {ComponentBase} from '../../drjs/browser/component.js';

export class FooterComponent extends ComponentBase{
    constructor(selector, htmlName='footer') {
        super(selector,htmlName);
    }
}

export default FooterComponent;